const commons = require('common.js');
        // var close = text('立即领取').findOnce();
        // toastLog(close)
        // if (close) {
        //     id('go').findOnce().click()
        //     sleep(1000);
        // }
        var t = id('go').findOnce();
        toastLog(t);
